// Preload script
// WhatsApp's native HTML5 notifications work well with Electron out of the box,
// but you can inject custom integration logic here if needed.
window.addEventListener('DOMContentLoaded', () => {
  console.log('WhatsApp wrapper loaded securely.');
});
